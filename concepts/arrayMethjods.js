const arr = new Array(5).fill(1).map((e) => [e]);

//new keyword creats a new array and you can specify the length of the array
// .fill will give you the ability to fill in the array.
// by using a map function i am creating a nested array.

// console.log(arr);

let Arr = [0,1,2,3,4,5];

console.log(Arr.splice(0,Arr.length));
